version: "3.9"

services:
  web:
    build: .
    container_name: "backend"
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    tty: true
